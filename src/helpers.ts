export const page = (path?: string) => {
  return {
    updateDynamicData: (data: any) => {
      // update dynamic data
    }
  }

}

export const action = (name: string) => {
  return {
    run: () => {
      // run action
    }
  }
}